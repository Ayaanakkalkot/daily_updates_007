# 2026-08-18

- Ownership tab — per-person Sentry load: who holds what, P1/P2/P3 split, what they cleared, oldest issue
- Releases tab — recent releases with how many bugs each introduced, commits/author, deploy env
- Access tab — admins & sub-admins grant assign / resolve to any of the 21 Sentry members, per person
- Split the permission model — assign (claim a bug) is now separate from resolve/ignore (close one); resolve implies assign
- Fixed why assignment silently failed — a stale admin-only rule hid the dropdown instead of erroring; found the identical bug a second time on Resolve/Ignore
- Fixed a mis-attribution bug — issues stored only the assignee's display name, so team assignments looked like people; added stable email + type
- Performance tracing: it's one env var, not a code change — SENTRY_TRACES_SAMPLE_RATE is already wired and defaults to 0
- Committed & pushed 3 features across 9c7381857, 881b6256d, 21bea3ea8 — rebased onto your PR #93 conflict resolution rather than force-pushing over it
