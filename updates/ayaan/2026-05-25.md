# 2026-05-25

- Continue with the integration of ELK in zFETCH people enrichment pipeline.
- Investigate where the Claude usages went for dev5 and FE Claude.
- jb_title case mismatch — ES stores titles as Title Case ("Data Engineer"), we were querying lowercase. Fixed by sending original-case variants.
- output_collection missing from task result — tasks.py FINISHED summary didn't include output_collection, so frontend always opened people_enrichment_output regardless of ELK prefix. Fixed by adding it to the summary dict.
- Frontend using stale local state for collection name — ProspectProfiles.tsx used its own searchSource state to derive the collection name. Now reads output_collection from the task status response (authoritative from backend).
- sort: [{"_id": "asc"}] dropping profiles — Sorting on ES _id is deprecated, silently returned only 4 of 9 profiles. Fixed by sorting on id (person UUID) and cmp_id (company UUID) — proper keyword fields. Now returns all 9.
